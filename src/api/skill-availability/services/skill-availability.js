'use strict';

/**
 * skill-availability service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::skill-availability.skill-availability');
