'use strict';

/**
 * skill-availability controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::skill-availability.skill-availability');
